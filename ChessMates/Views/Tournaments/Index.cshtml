@model IEnumerable<ChessMates.Models.Tournament>
@{
    ViewBag.Title = "Tournaments";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Tournaments</h2>

<table id="tournaments" class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Name</th>
            <th>City</th>
            <th>Start</th>
            <th>End</th>
            @*<th>Delete</th>*@
        </tr>
    </thead>
    <tbody></tbody>
</table>

@section scripts
{
<script>
        $(document).ready(function() {
            var table = $("#tournaments").DataTable({
                ajax: {
                    url: "/api/tournaments",
                    dataSrc: ""
                },
                columns: [
                    {
                        data: "Name"
                    },
                    {
                        data: "City"
                    },
                    {
                        data: "StartDate"
                        @*@Html.TextBoxFor(m => m.ReleaseDate, "{0:d MMM yyyy}", new { @class = "form-control" })*@
                    },
                    {
                        data: "EndDate"
                    },
                    @*{
                        data: "Id",
                        render: function(data) {
                            return "<button class='btn-link js-delete' data-tournament-id=" + data + ">Delete</button>";
                        }
                    }*@
                ]
            });


            $("#tournaments").on("click",
                ".js-delete",
                function() {
                    var button = $(this);

                    bootbox.confirm("Are you sure you want to delete this tournament?",
                        function(result) {
                            if (result) {
                                $.ajax({
                                    url: "/api/tournaments/" + button.attr("data-tournament-id"),
                                    //url: "/api/tournaments/",
                                    method: "DELETE",
                                    success: function() {
                                        table.row(button.parents("tr")).remove().draw();
                                    }
                                });
                            }
                        });
                });
        });
</script>

}