@model IEnumerable<ChessMates.Models.Tournament>
@{
    ViewBag.Title = "Tournaments";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Tournaments</h2>

<table id="tournaments" class="table table-bordered table-hover" style="background-color:aliceblue">
    <thead>
        <tr style="background-color:royalblue; color:white">
            <th>Name</th>
            <th>City</th>
            <th>Start</th>
            <th>End</th>
            <th>Delete</th>
        </tr>
    </thead>
    <tbody></tbody>
</table>

@section scripts
{
<script>
        $(document).ready(function() {
            var table = $("#tournaments").DataTable({
                ajax: {
                    url: "/api/Tournaments",
                    dataSrc: ""
                },
                columns: [
                    {
                        data: "Name"
                    },
                    {
                        data: "City"
                    },
                    {
                        data: "StartDate"
                        @*@Html.TextBoxFor(m => m.ReleaseDate, "{0:d MMM yyyy}", new { @class = "form-control" })*@
                    },
                    {
                        data: "EndDate"
                    },
                    {
                        data: "Id",
                        render: function (data) {
                            console.log(data);
                            return "<button class='btn btn-info js-details' data-tournament-id=" + data + "><i class='fa fa-info-circle'></i></button><span width='5px'></span><button class='js-delete btn btn-danger' data-tournament-id=" + data + "><i class='fa fa-trash-o'></i></button>";
                        }
                    }
                ]
            });


            $("#tournaments").on("click",
                ".js-delete",
                function() {
                    var button = $(this);

                    bootbox.confirm("Are you sure you want to delete this tournament?",
                        function(result) {
                            if (result) {
                                $.ajax({
                                    url: "/api/Tournaments/" + button.attr("data-tournament-id"),
                                    //url: "/api/tournaments/",
                                    method: "DELETE",
                                    success: function() {
                                        table.row(button.parents("tr")).remove().draw();
                                    }
                                });
                            }
                        });
                });
        });
</script>

}